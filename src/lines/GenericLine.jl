abstract type Line()
    """
    # ! Each line exactly one input and one output (connecting two nodes)
    # ? use line model from PowerDynamics.jl:
    #       --> StaticLine and Transformer
    """
end # type
